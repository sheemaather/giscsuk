document.getElementById("attendanceForm").onsubmit=async e=>{e.preventDefault();const roll=document.getElementById("roll").value.trim();const box=document.getElementById("attendanceResult");box.innerHTML='<div class="spinner-border"></div>';try{const r=await fetch("/api/attendance/student/"+encodeURIComponent(roll));const d=await r.json();if(!r.ok)throw Error(d.message);const present=d.filter(x=>x.status==="Present"||x.status==="Late").length,total=d.filter(x=>x.attendance_date).length,pct=total?Math.round(present/total*100):0;box.innerHTML=`<div class="student-summary"><div><small>Student</small><h4>${d[0].name}</h4><span>${d[0].roll_number} • Semester ${d[0].semester}</span></div><div class="attendance-percent">${pct}%</div></div><div class="table-responsive mt-4"><table class="table"><thead><tr><th>Date</th><th>Check in</th><th>Check out</th><th>Status</th></tr></thead><tbody>${d.filter(x=>x.attendance_date).map(x=>`<tr><td>${x.attendance_date}</td><td>${x.check_in||"-"}</td><td>${x.check_out||"-"}</td><td>${x.status}</td></tr>`).join("")}</tbody></table></div>`}catch(err){box.innerHTML=`<div class="alert alert-warning">${err.message}</div>`}};
